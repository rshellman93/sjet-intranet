@extends('layout')
@section('title','MUS-samtale')
@section('content')
@php($isLeader=(int)$conversation->leader_id===auth()->id())
@php($archived=$conversation->status==='Arkiveret')
<div class="page-head"><div><div class="eyebrow">FORTROLIG SAMTALE · SKABELON {{ $conversation->template_version }}</div><h1>{{ $conversation->employee_name }}</h1><p>{{ $conversation->job_title }} · Samtaleleder: {{ $conversation->leader_name }}<br>Planlagt {{ \App\Support\DateFormat::date($conversation->scheduled_on) }}</p></div><div class="actions"><span class="pill">{{ $conversation->status }}</span><a class="btn secondary" href="{{ route('mus.index') }}">Mine MUS</a></div></div>
@if($conversation->scheduled_on < now()->addDays(7)->toDateString() && !$conversation->held_on)<p class="notice">Kort forberedelsestid: aftal gerne mindst syv dages varsel. Afsæt cirka ti minutter til forberedelse og 55–60 minutter til samtalen.</p>@endif
@if(count($previousGoals))<details class="card disclosure"><summary>Tidligere åbne aftaler · {{ count($previousGoals) }}</summary>@foreach($previousGoals as $goal)<div class="info-row"><div><h3>{{ $goal['result'] }}</h3><p>{{ $goal['status'] }} · Frist {{ $goal['deadline'] }}</p></div><a href="{{ route('mus.show',$goal['conversation']) }}">Åbn tidligere MUS ↗</a></div>@endforeach</details>@endif
<div class="savebar"><span id="save-status" role="status" aria-live="polite">Servergemt kladde</span><div class="actions"><button type="button" class="btn secondary small" id="save-retry">Gem nu</button><button type="button" class="btn secondary small" id="save-copy">Hent lokal kopi</button></div></div><p id="save-error" class="alert error" hidden></p>
<nav class="mus-steps" aria-label="MUS-trin">@foreach(['Forberedelse','Fælles vurdering','Samtale','Aftaler'] as $step=>$label)<button type="button" data-step-button="{{ $step }}"><span>{{ $step+1 }}</span>{{ $label }}</button>@endforeach</nav>
<form id="mus-form" data-allow-repeat method="post" action="{{ route('mus.save',$conversation->id) }}">@csrf<input type="hidden" name="revision" value="{{ $conversation->revision }}"><input type="hidden" name="preparation_revision" value="{{ $preparation?->revision ?? 0 }}">
<fieldset class="mus-content" @disabled($archived)>
<section data-mus-step="0" class="card"><div class="eyebrow">TRIN 1 AF 4</div><h2>Forberedelse</h2><p class="notice">Notér kun nødvendige, arbejdsrelevante oplysninger. Lønforhandling og private personalesager hører ikke hjemme i MUS.</p>
@unless($isLeader)<span class="pill {{ $preparation?->shared_at ? '':'inactive' }}">{{ $preparation?->shared_at ? 'Din forberedelse er delt':'Privat – kun synlig for dig' }}</span><h3>A · Min puls lige nu</h3><p class="muted">Vurder fra 1 til 5. Du kan lade felter stå ubesvaret.</p>@include('mus.ratings',['labels'=>\App\Support\MusWorkflow::PULSE,'prefix'=>'preparation[pulse]','values'=>$own['pulse'] ?? []])<h3>B · Mit udgangspunkt</h3>@foreach(\App\Support\MusWorkflow::START as $i=>$label)<label>{{ $label }}<textarea name="preparation[start][{{ $i }}]" maxlength="4000">{{ $own['start'][$i] ?? '' }}</textarea></label>@endforeach
@else
<p>Som samtaleleder kan du registrere medarbejderens svar under en fælles samtale på samme iPad.</p><div class="notice">Svarene her er fælles samtaleregistreringer af {{ auth()->user()->name }}. De erstatter ikke medarbejderens private forberedelse eller bekræftelse via eget login.</div><h3>A · Medarbejderens puls · fælles registrering</h3>@include('mus.ratings',['labels'=>\App\Support\MusWorkflow::PULSE,'prefix'=>'shared[joint][pulse]','values'=>$shared['joint']['pulse'] ?? []])<h3>B · Medarbejderens udgangspunkt · fælles registrering</h3>@foreach(\App\Support\MusWorkflow::START as $i=>$label)<label>{{ $label }}<textarea name="shared[joint][start][{{ $i }}]" maxlength="4000">{{ $shared['joint']['start'][$i] ?? '' }}</textarea></label>@endforeach
@endunless
</section>
<section data-mus-step="1" class="card"><div class="eyebrow">TRIN 2 AF 4</div><h2>C · Adfærd i hverdagen</h2><span class="pill {{ $preparation?->shared_at ? '':'inactive' }}">{{ $preparation?->shared_at ? 'Din vurdering er delt':'Din vurdering er privat indtil deling' }}</span><p class="muted">Din egen vurdering som {{ $isLeader ? 'leder':'medarbejder' }}. Ingen samlet personscore.</p>@include('mus.ratings',['labels'=>\App\Support\MusWorkflow::BEHAVIOUR,'prefix'=>'preparation[behaviour]','values'=>$own['behaviour'] ?? []])
@if($isLeader)<details class="disclosure"><summary>Registrér medarbejderens vurdering sammen på iPad</summary><p class="notice">Registreret af {{ auth()->user()->name }} under den fælles samtale. Ikke en selvstændig medarbejderbekræftelse.</p>@include('mus.ratings',['labels'=>\App\Support\MusWorkflow::BEHAVIOUR,'prefix'=>'shared[joint][behaviour]','values'=>$shared['joint']['behaviour'] ?? []])</details>@endif
@if(!$isLeader && !empty($shared['joint']))<details class="disclosure"><summary>Fælles registrering af dine svar ved {{ $conversation->leader_name }}</summary><p class="notice">Registreret under samtalen af lederen. Din egen forberedelse og bekræftelse er særskilte.</p>@foreach(['pulse'=>\App\Support\MusWorkflow::PULSE,'start'=>\App\Support\MusWorkflow::START,'behaviour'=>\App\Support\MusWorkflow::BEHAVIOUR] as $key=>$labels) @foreach($labels as $i=>$label)<h3>{{ $label }}</h3><p class="preserve">{{ $shared['joint'][$key][$i] ?? 'Ubesvaret' }}</p>@endforeach @endforeach</details>@endif<h3>Delte vurderinger side om side</h3>@if(!$other)<p class="muted">Den anden deltager har endnu ikke delt sin forberedelse.</p>@else<p class="muted">Delt {{ \App\Support\DateFormat::dateTime($other->shared_at) }} · Senest ændret {{ \App\Support\DateFormat::dateTime($other->updated_at) }}</p>@endif
<div class="table-wrap"><table><thead><tr><th>Område</th><th>Din vurdering</th><th>Anden deltager</th><th>Forskel</th></tr></thead><tbody>@foreach(\App\Support\MusWorkflow::BEHAVIOUR as $i=>$label) @php($a=$own['behaviour'][$i] ?? null) @php($b=$otherAnswers['behaviour'][$i] ?? null)<tr><td>{{ $label }}</td><td>{{ $a ?: 'Ubesvaret' }}</td><td>{{ $b ?: 'Ikke delt / ubesvaret' }}</td><td>{{ $a && $b ? ($a==$b ? 'Samme vurdering':abs($a-$b).' trin'):'—' }}</td></tr>@endforeach</tbody></table></div>
@if($other && !empty($otherAnswers['start']))<details class="disclosure"><summary>Den anden deltagers delte udgangspunkt</summary>@foreach(\App\Support\MusWorkflow::START as $i=>$label)<h3>{{ $label }}</h3><p class="preserve">{{ $otherAnswers['start'][$i] ?? 'Ubesvaret' }}</p>@endforeach</details>@endif
</section>
<section data-mus-step="2" class="card"><div class="eyebrow">TRIN 3 AF 4 · FÆLLES OG SYNLIGT FOR BEGGE</div><h2>Samtalen</h2>
@foreach(['D · Tal om forskellene'=>['same','difference','behaviour'],'E · Resultater og feedback'=>['results','next_level','leader_feedback'],'F · Udvikling gennem arbejdet'=>['ambition','training']] as $heading=>$keys)<h3>{{ $heading }}</h3>@foreach($keys as $key)<label>{{ \App\Support\MusWorkflow::FIELDS[$key] }}<textarea name="shared[{{ $key }}]" maxlength="4000">{{ $shared[$key] ?? '' }}</textarea></label>@endforeach @endforeach
<div class="form-grid"><label>Foretrukken udviklingsretning<select name="shared[direction]"><option value="">Ikke valgt</option>@foreach(\App\Support\MusWorkflow::DIRECTION as $direction)<option @selected(($shared['direction'] ?? '')===$direction)>{{ $direction }}</option>@endforeach</select></label><label>Kompetencen, der trænes<select name="shared[training_competency_id]"><option value="">Ikke valgt</option>@foreach($competencies as $cid=>$label)<option value="{{ $cid }}" @selected(($shared['training_competency_id'] ?? '')==$cid)>{{ $label }}</option>@endforeach</select></label></div><p class="muted">En sagsreference fra Minuba kan skrives i træningsopgaven.</p>
</section>
<section data-mus-step="3" class="card"><div class="eyebrow">TRIN 4 AF 4 · FÆLLES OG SYNLIGT FOR BEGGE</div><h2>G · Højst tre prioriterede mål</h2><p class="muted">Udfyld kun de mål, I faktisk aftaler. Overførsel til kompetencekortet sker først efter begge deltageres bekræftelse.</p>
@for($i=0;$i<3;$i++) @php($goal=$shared['goals'][$i] ?? [])<details class="goal-editor" @if($i===0 || !empty($goal['result'])) open @endif><summary>Mål {{ $i+1 }}{{ !empty($goal['result']) ? ' · '.$goal['result']:'' }}</summary><label>Ønsket resultat<textarea name="shared[goals][{{ $i }}][result]" maxlength="2000">{{ $goal['result'] ?? '' }}</textarea></label><div class="form-grid"><label>Første konkrete handling<textarea name="shared[goals][{{ $i }}][action]" maxlength="2000">{{ $goal['action'] ?? '' }}</textarea></label><label>Tegn på fremdrift<textarea name="shared[goals][{{ $i }}][evidence]" maxlength="2000">{{ $goal['evidence'] ?? '' }}</textarea></label><label>Ejer<select name="shared[goals][{{ $i }}][owner_id]">@foreach([$conversation->employee_id=>$conversation->employee_name,$conversation->leader_id=>$conversation->leader_name] as $uid=>$name)<option value="{{ $uid }}" @selected(($goal['owner_id'] ?? $conversation->employee_id)==$uid)>{{ $name }}</option>@endforeach</select></label><label>Deadline<input type="date" name="shared[goals][{{ $i }}][deadline]" value="{{ $goal['deadline'] ?? '' }}"></label><label>Ønsket adfærd og praksis / kort faglig beskrivelse<textarea name="shared[goals][{{ $i }}][practice]" maxlength="2000">{{ $goal['practice'] ?? '' }}</textarea></label><label>Hvad skal aftalen oprette?<select name="shared[goals][{{ $i }}][transfer]">@foreach(['mus'=>'Kun MUS-aftale','competency'=>'Kompetenceudvikling','course'=>'Kursusbehov','both'=>'Både kompetenceudvikling og kursusbehov'] as $key=>$label)<option value="{{ $key }}" @selected(($goal['transfer'] ?? 'mus')===$key)>{{ $label }}</option>@endforeach</select></label><label>Målkompetence<select name="shared[goals][{{ $i }}][competency_id]"><option value="">Ingen valgt</option>@foreach($competencies as $cid=>$label)<option value="{{ $cid }}" @selected(($goal['competency_id'] ?? '')==$cid)>{{ $label }}</option>@endforeach</select></label><label>Ønsket niveau<select name="shared[goals][{{ $i }}][desired_level]"><option value="">Ikke fastlagt</option>@foreach(array_slice(\App\Support\Development::LEVELS,1,null,true) as $level=>$label)<option value="{{ $level }}" @selected(($goal['desired_level'] ?? '')==$level)>{{ $label }}</option>@endforeach</select></label><label>Kursusemne<input name="shared[goals][{{ $i }}][course_topic]" maxlength="190" value="{{ $goal['course_topic'] ?? '' }}"></label><label>Eksisterende åbent kursusbehov<select name="shared[goals][{{ $i }}][link_course_id]"><option value="">Nyt behov / bevar kobling for samme emne</option>@foreach($openCourses as $course)<option value="{{ $course->id }}" @selected(($goal['link_course_id'] ?? '')==$course->id)>#{{ $course->id }} · {{ $course->topic }} · {{ $course->status }}</option>@endforeach</select><small>Hvis behovet allerede findes, vælg det her for at undgå dobbeltregistrering.</small></label><label>Lederens konkrete støtte<textarea name="shared[goals][{{ $i }}][support]" maxlength="2000">{{ $goal['support'] ?? '' }}</textarea></label><label>Lederen leverer støtten senest<input type="date" name="shared[goals][{{ $i }}][support_due]" value="{{ $goal['support_due'] ?? '' }}"></label></div></details>@endfor
<label>Hvis ingen mål aftales: kort begrundelse<textarea name="shared[no_goals_reason]" maxlength="4000">{{ $shared['no_goals_reason'] ?? '' }}</textarea></label><h2>H · Lederens forpligtelse og opfølgning</h2><label>Konkret lederstøtte samlet<textarea name="shared[leader_support]" maxlength="4000">{{ $shared['leader_support'] ?? '' }}</textarea></label><div class="form-grid">@foreach(['held_on'=>'Faktisk samtaledato','checkin_on'=>'30-dages check-in','followup_on'=>'90-dages opfølgning','next_mus_on'=>'Næste MUS'] as $key=>$label)<label>{{ $label }}<input type="date" name="shared[{{ $key }}]" value="{{ $shared[$key] ?? '' }}"></label>@endforeach</div><button type="button" class="btn secondary" id="suggest-dates">Foreslå datoer fra samtaledatoen</button><div id="draft-preview" class="transfer-preview" aria-live="polite"></div>
</section>
</fieldset><div class="wizard-actions"><button type="button" class="btn secondary" id="previous-step">← Tilbage</button><button type="submit" class="btn secondary">Gem nu</button><button type="button" class="btn" id="next-step">Næste →</button></div>
</form>
@unless($archived)<div class="actions mus-actions">@unless($preparation?->shared_at)<form method="post" action="{{ route('mus.share',$conversation->id) }}" data-sync-mus>@csrf<button type="submit" class="btn secondary">Del min private forberedelse</button></form>@endunless<form method="post" action="{{ route('mus.publish',$conversation->id) }}" data-sync-mus>@csrf<input type="hidden" name="revision" value="{{ $conversation->revision }}"><button type="submit" class="btn">Klargør aftaleversion til bekræftelse</button></form></div>@endunless
@if($latest)<section class="card" id="agreement"><div class="section-head"><h2>Gennemgå aftaleversion {{ $latest->version }}</h2><a class="btn secondary small" href="{{ route('mus.print',[$conversation->id,$latest->version]) }}">Udskriftsvisning ↗</a></div>
@if((int)$conversation->submitted_revision!==(int)$conversation->revision)<p class="alert error">Kladderne er ændret siden denne version. Klargør en ny version, før begge bekræfter.</p>@endif
@include('mus.agreement',['agreement'=>\App\Support\MusWorkflow::decode($latest->snapshot)['shared']])
<div class="confirmation-list">@foreach($confirmations as $confirmation)<p>✓ {{ $confirmation->name }} · eget login · {{ \App\Support\DateFormat::dateTime($confirmation->confirmed_at) }} · version {{ $latest->version }}</p>@endforeach</div>
@if(!$archived && (int)$conversation->submitted_revision===(int)$conversation->revision && !$confirmations->contains('user_id',auth()->id()))<form method="post" action="{{ route('mus.confirm',$conversation->id) }}" data-sync-mus>@csrf<input type="hidden" name="version" value="{{ $latest->version }}"><label class="check"><input type="checkbox" name="consent" value="1" required>Jeg, {{ auth()->user()->name }}, bekræfter version {{ $latest->version }} og den viste overførsel til KLS.</label><button type="submit" class="btn">Bekræft via mit eget login</button></form>@endif
@if($conversation->confirmed_version)<p class="notice">Seneste fælles bekræftelse: version {{ $conversation->confirmed_version }}. En ny kladde ændrer ikke det tidligere bekræftede grundlag.</p>@endif</section>@endif
@if($confirmed) @php($confirmedGoals=\App\Support\MusWorkflow::decode($confirmed->snapshot)['shared']['goals'])
<section class="card" id="followups" style="margin-top:24px"><h2>Aftaler og opfølgning</h2><p class="muted">Baseret på den fælles bekræftede version {{ $confirmed->version }}.</p>@foreach($confirmedGoals as $goal) @php($state=$goalStatus->get($goal['id']))<div class="info-row"><div><h3>{{ $goal['result'] }}</h3><p>Frist {{ $goal['deadline'] }} · {{ $state?->status }} @if($goal['deadline'] < now()->toDateString() && !in_array($state?->status,['Gennemført','Udgået'])) · Forfalden @endif</p><p>Lederstøtte: {{ $state?->support_delivered_on ? 'Leveret '.$state->support_delivered_on : 'Afventer · frist '.$goal['support_due'] }}</p>@if($links->has($goal['id']))<p>KLS-kursus: {{ $links[$goal['id']]->topic }} · {{ $links[$goal['id']]->status }}</p>@endif</div></div>@endforeach
@foreach($followups as $followup)<details class="disclosure"><summary>Opfølgning {{ \App\Support\DateFormat::date($followup->due_on) }} · {{ $followup->completed_on ? 'Gennemført' : ($followup->due_on < now()->toDateString() ? 'Forfalden':'Planlagt') }}</summary>
@if($followup->completed_on) @php($progress=\App\Support\MusWorkflow::decode($followup->progress))<p>Registreret {{ $followup->completed_on }}</p>@foreach($progress['goals'] ?? [] as $entry)<div class="notice"><p>{{ $entry['status'] }} · Fremdrift: {{ $entry['progressed'] ? 'Ja':'Nej' }}</p><p class="preserve">{{ $entry['note'] }}</p></div>@endforeach<p>Blokeringer: {{ $progress['blockers'] ?? 'Ingen' }}</p><p>Næste skridt: {{ $progress['next_step'] ?? '' }}</p>
@elseif(!$archived && $isLeader)<form method="post" action="{{ route('mus.followup',[$conversation->id,$followup->id]) }}">@csrf<input type="hidden" name="version" value="{{ $confirmed->version }}"> @foreach($confirmedGoals as $goal)<h3>{{ $goal['result'] }}</h3><div class="form-grid"><label>Status<select name="goals[{{ $goal['id'] }}][status]">@foreach(\App\Support\MusWorkflow::GOAL_STATUSES as $status)<option @selected($goalStatus->get($goal['id'])?->status===$status)>{{ $status }}</option>@endforeach</select></label><label>Er der dokumenteret fremdrift?<select name="goals[{{ $goal['id'] }}][progressed]" required><option value="">Vælg</option><option value="1">Ja</option><option value="0">Nej</option></select></label></div><label>Kort begrundelse / dokumenteret fremdrift<textarea name="goals[{{ $goal['id'] }}][note]" required maxlength="2000"></textarea></label><label class="check"><input type="checkbox" name="goals[{{ $goal['id'] }}][support_done]" value="1">Lederens støtte er leveret</label>@endforeach<label>Eventuelle blokeringer<textarea name="blockers" maxlength="2000"></textarea></label><label>Næste skridt<textarea name="next_step" required maxlength="2000"></textarea></label><label>Næste opfølgning – valgfri<input type="date" name="next_due"><small>Ved 90-dages opfølgning kan I aftale næste kvartalsvise opfølgning. Samme dato oprettes kun én gang.</small></label><button type="submit" class="btn">Gem opfølgning</button></form>@endif</details>@endforeach</section>@endif
<details class="card disclosure"><summary>Versioner og historik</summary>@foreach($versions as $version)<p><a href="{{ route('mus.print',[$conversation->id,$version->version]) }}">Aftaleversion {{ $version->version }} · {{ $version->created_at }} ↗</a></p>@endforeach @foreach($history as $event)<div class="info-row"><span>{{ $event->event }} · {{ $event->actor }}</span><small>{{ \App\Support\DateFormat::dateTime($event->created_at) }}</small></div>@endforeach</details>
@if($isLeader && !$archived)<form method="post" action="{{ route('mus.archive',$conversation->id) }}" style="margin-top:24px">@csrf<button type="submit" class="btn secondary">Arkivér forløb og bevar historik</button></form>@endif
<script src="/mus.js" defer></script>
@endsection
